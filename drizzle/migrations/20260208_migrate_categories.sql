-- Migration: Convert product categories from string to foreign key reference
-- This migration is now a NO-OP as the schema is already up to date on the remote database.
-- Product categories have already been migrated to the categories table.

SELECT 1;
