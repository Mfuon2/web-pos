-- Migration: Separate stock counts from products table
-- This migration is now a NO-OP as the schema is already up to date on the remote database.
-- The stock table already exists and contains the necessary data.

SELECT 1;
